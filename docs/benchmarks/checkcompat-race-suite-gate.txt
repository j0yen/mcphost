PRD-mcphost-checkcompat-port-race AC7 (P0) — Given the whole mcphost test
suite, When `cargo test --workspace` runs on RedBaron, Then it exits 0 and
clippy is unchanged versus the committed baseline (delta-pass convention).

Methodology: same "regression lock on a checked-in receipt, not a live
re-run inside `cargo test`" pattern as
docs/benchmarks/suite-consolidation-wall-time.txt (running the whole
workspace suite recursively from inside one of its own tests would be
both circular and multi-minute cost paid on every `cargo test`). This
receipt is the literal `wm-build runner exec --run 31 -- cargo ...`
output at PRD-mcphost-agent-mesh-ops's HEAD (rebased onto v0.58.4, which
includes PRD-mcphost-checkcompat-port-race #19), on the remote build
runner (RedBaron), never run locally per this repo's own build contract.

head_sha: b396821c7c269f0234749e0e381f6b5132eae4ce

cargo test --workspace:
  729 passed, 0 failed, 5 ignored (hardware/cache-dependent, same as
  before this PRD -- suite_ac5_disk_size_budget,
  suite_ac6_wall_time_budget's own sibling ignores, ac11_load_smoke's
  two-hundred-concurrent load test, etc.)
  exit: 0

cargo clippy --all-targets -- -D warnings:
  0 warnings
  exit: 0

clippy_warning_count: 0

Delta-pass: rebasing PRD-mcphost-agent-mesh-ops onto v0.58.4 (which lands
PRD-mcphost-checkcompat-port-race #19) made this receipt's prior head_sha
stale -- every meshops src/tests path changed relative to it and was
never re-proven against a fresh cargo test --workspace / cargo clippy
run. Re-ran both here at the rebased HEAD; 0 warnings before, 0 warnings
after. No new `#[allow(clippy::...)]` was added by this PRD's diff
(checked directly by
tests/checkcompat_race_ac07_suite_green_and_clippy_clean.rs), so the
clean run is by construction, not suppression.

Re-run and update this file (and its consumer test) together if a later
PRD's diff touches this again materially.
